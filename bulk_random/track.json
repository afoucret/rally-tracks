{% import "rally.helpers" as rally with context %}
{
  "version": 2,
  "description": "Prof of concept for randomized doc indexing",
  "indices": [
    {
      "name": "random-doc",
      "body": "index-mapping.json"
    }
  ],
  "operations": [
    {{ rally.collect(parts="operations/*.json") }}
  ],
  "challenges": [
    {{ rally.collect(parts="challenges/*.json") }}
  ]
}
